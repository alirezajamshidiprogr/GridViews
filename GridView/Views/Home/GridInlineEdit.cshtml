@using GridView.ViewModel;

@model List<ProductModel>


<link href="~/css/gridinlineedit.css" rel="stylesheet" />

<grid-inline-edit fetch-url="@Url.Action("GetGridData", "Home")"
                  enable-paging="true"
                  enable-filtering="true"
                  enable-sorting="true"
                  enable-grouping="true"
                  model-type="typeof(ProductModel)">
</grid-inline-edit>

<script src="~/js/gridinlineedit.js"></script>